/*
 * PLACEHOLDER — vendor the real htmx.min.js here at deploy time:
 *   curl -o web/static/htmx.min.js https://unpkg.com/htmx.org@1.9.12/dist/htmx.min.js
 * The UI is fully functional without it (forms fall back to normal GET
 * submits); htmx only adds in-place table swaps on filter change.
 * Vendored locally on purpose: the deployment is VPN-only with no
 * public-internet/CDN dependency.
 */
